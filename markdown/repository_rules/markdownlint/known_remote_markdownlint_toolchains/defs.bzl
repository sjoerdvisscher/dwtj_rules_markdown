# NOTE(dwtj): This isn't currently technically a repository rule. It is
#  currently a macro which declares other repository rule instances. In the
#  future this might instead by implemented as a propert repository rule.
def known_remote_markdown_lint_toolchains():
    print("TODO(dwtj): Everything!")
    pass